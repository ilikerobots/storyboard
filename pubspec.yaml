name: storyboard
description: Faster iterative development of Flutter user interfaces by isolating widgets from your app.
version: 1.0.0
homepage: https://github.com/ilikerobots/storyboard

dependencies:
  flutter:
    sdk: flutter
  recase: ^4.0.0-nullsafety.0

dev_dependencies:
  flutter_test:
    sdk: flutter


environment:
  sdk: ">=2.12.0 <3.0.0"

flutter:
